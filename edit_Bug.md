07-May-26
แก้ไข 1
 ให้ปรับเพิ่มจาก session อันเดิมให้เพิ่มเวลาและชื่อนามสกุลจริงของลูกค้า(โดยชื่อนามสกุลจริงให้ลูกค้ากรอกเอง) และเปลี่ยนจากกดแท็บsessionเป็นการเพิ่มedit session ขึ้นมาเพื่อกดแทนการกดแท็บ

แก้ไข 2 เมื่อกดแท็บ session ID จะแสดง 5 ตัวนี้ตามที่เราเขียนกันไป
   1.Plant tour information
   2.Audit Time Table
   3.Audit Preparation material
   4.Audit Report (customer)
   5.Audit Report (Internal)
ข้อ 5 ย่อยนี้ต้องสามารถเพิ่มไฟล์ได้มากกว่า 1 ไฟล์ และสามารถเขียนDetailได้ไว้ว่าไฟล์นั้นคืออะไร สามารถ edit detail ของแต่ละไฟล์ได้ และDetailมันจะขึ้นที่บนไฟล์เมื่ออัปโหลดไฟล์แล้ว และแต่ละไฟล์ต้องเรียงลำดับ มาก่อนอยู่บน มาหลังอยู่ล่าง
 
แก้ไข 3 
  มี bug ที่ต้องแก้ ดังนี้
  1) ในแท็บตรง ปี คศ เมื่อกด จะแสดงชื่อลูกค้า กดที่ชื่อลูกค้าจะมี แท็บ sessionID เมื่อกดจะแสดง 1.Plant tour information 2.Audit Time Table 3.Audit Preparation material 4.Audit Report (customer) 5.Audit Report (Internal)
     จุดbugก็คือ ทำไมมันต้องเปิด 5 ข้อนี้ไว้ในแต่ละ sessionID เพื่อเวลาปิดแท็บมันจะปิด5ข้อนั้นด้วย แต่เวลาเราปิดหัวข้อ5ข้อนั้นไว้พอกดปิดแท็บsessionของลูกค้าให้เหลือแต่ปี คศ ทำไม5ข้อนั้นยังคงแสดง(เช่น ปิดแท็บsessionID 2 แท็บลูกค้า กดซ่อนชื่อลูกค้าหัวข้อทั้ง5จะโผล่มา2ชุดคือ10บรรทัด)
  2) ตรง save file เวลากดแล้วจะขึ้นสถานะ loading... (ถูกแล้ว ระบบเพิ่มเรียบร้อย) แต่คำมันค้างอยู่แบบนั้นเวลากดไฟล์อื่นไปแล้ว ต้องกดรีเฟรชก่อนถึงจะหาย ต้องแก้เพราะ User จะงง และไฟล์เก่าก่อนหน้ายังค้างที่ช่อง upload 
  3) ตอนระบบกำลัง delete / save file กดแล้วตอนมันกำลัง up link ไป sheet จะแสดงสถานะ  deleting.../updating... ที่หน้าตารางเลย ซึ่งทางจิตวิทยาแล้วมันจะทำให้ user ตกใจ อยากเปลี่ยนหน้าต่างให้เป็นpopเล็กๆขึ้นมาให้รู้สถานะว่ากำลัง deleting.../updating... ไม่ต้องไปแสดงทับบนตารางdata ให้ตารางแสดงdataของมันแบบrealtimeไป

แก้ไข 4
  เวลากดลบไฟล์บนหน้าเว็บแล้ว แต่ในDrivไม่ยอมลบให้

07-May-2026
1) แก้ 01 Audit Schedule เอา year และ Customer Name ออก ให้เหลือแค่ Contact Person (Full Name) และ  Document Title บันทึกแล้วแต่อยากแก้ชื่อtitleเผื่อเขียนผิด สามารถเพิ่มสีในช่องตารางที่สร้างในไฟล์Docได้ ให้มีการกด refresh หน้า  View Document ได้เพราะบางทีที่แก้มันยังโหลดมาหน้านี้เดี๋ยวUserจะตกใจและเขียนซ้ำ
2) แก้ 02 Audit Result 1)ไม่มีการแสดงชื่อลูกค้าที่ดึงชื่อผู้ติดต่อมาจาก Customer_Real_Name จาก Data ในชีต 02_Audit Result ใน Google sheet 2)Edit detail ไม่ได้ 4)เพิ่มปีแล้ว เพิ่มDataบริษัทที่หนึ่งในปีนั้นแล้ว แต่ไม่ปุ่มให้เพิ่มบริษัทถัดไปในปีนั้นๆเพื่อความสะดวก
3) แก้ โปรแกรม word doc ให้เปลี่ยนสีแต่ละช่อง (Cell) ได้แบบเจาะจง โดนถ้าคุณต้องการคุมโทนสี (เช่น สีวันหยุดต้องแดงเฉดนี้เท่านั้น)
4) อัพรูปจากเครื่องลงไดร์ฟได้แล้วแต่มันไม่แสดงบน Word Doc ที่ทำทั้งตอนที่ edit และตอนที่ View Document
5) พอเข้ากดEditแล้วลบรูป พอกดsav Doc มันแสดง Document is empty!

//ถามเพิ่มชื่อ url => https://script.google.com/ แก้ชื่อได้ไหม
